#include "main.h"

/**
 * _abs - calculates the exact value of an integer
 * @t: The integer to be worked out
 * Return: The absolute value.
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

